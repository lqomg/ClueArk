# ClueArk 全栈单机部署（MongoDB + 后端 + gateway + 前端 Nginx）
#
# 首次准备：
#   1. cp .env.example .env，按需填写（生产务必设置 JWT_SECRET、ADMIN_PASSWORD、Mongo root 密码等）
#   2. 首次部署（或要重置数据库）执行：docker compose down -v
#
# 启动：
#   docker compose up -d --build
#
# 访问：
#   用户产品 https://clueark.com（或 http://<host>，经 gateway 默认站点）
#   运营后台 https://admin.clueark.com（仅 admin 登录）
#   域名与 Cloudflare 配置见 deploy/nginx/README.md
#
# gateway 容器按 Host 分流；web/admin-web 容器内 Nginx 将 /api 反代到 backend。
#
# 爬虫服务默认随栈启动（闭环抓取 Web 信源并上报主站）。

services:
  gateway:
    image: nginx:1.27-alpine
    container_name: clueark-gateway
    restart: unless-stopped
    ports:
      - "${GATEWAY_BIND:-0.0.0.0}:${GATEWAY_PORT:-80}:80"
    volumes:
      - ./deploy/nginx/gateway.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      web:
        condition: service_started
      admin-web:
        condition: service_started
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://127.0.0.1/']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

  redis:
    image: redis:7-alpine
    container_name: clueark-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - clueark_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:latest
    container_name: clueark-qdrant
    restart: unless-stopped
    ports:
      - "0.0.0.0:${QDRANT_BIND_PORT:-6333}:6333"
    volumes:
      - clueark_qdrant_data:/qdrant/storage
    healthcheck:
      # qdrant/qdrant 镜像不含 wget/curl，用 bash /dev/tcp 探测 /readyz
      test:
        [
          "CMD-SHELL",
          "bash -c \"exec 3<>/dev/tcp/127.0.0.1/6333 && echo -e 'GET /readyz HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && grep -q 'HTTP/1.1 200' <&3\"",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  mongodb:
    image: mongo:7
    container_name: clueark-mongodb
    restart: unless-stopped
    command: ["mongod", "--auth", "--bind_ip_all"]
    ports:
      - "0.0.0.0:${MONGO_BIND_PORT:-27017}:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:-root}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:-change_me_root_password}
    volumes:
      - clueark_mongodb_data:/data/db
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mongosh --quiet -u \"$MONGO_INITDB_ROOT_USERNAME\" -p \"$MONGO_INITDB_ROOT_PASSWORD\" --authenticationDatabase admin --eval \"db.adminCommand('ping').ok\" | grep -q 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: clueark-backend
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-production}
    env_file:
      - path: ./.env
        required: true
    volumes:
      - ./backend/logs:/app/logs
      - ./backend/uploads:/app/uploads
      - ./data:/data:ro
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      worker:
        condition: service_started
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://127.0.0.1:3000/api/health/ready']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: clueark-worker
    restart: unless-stopped
    command: ['dumb-init', 'node', 'dist/worker']
    environment:
      NODE_ENV: ${NODE_ENV:-production}
    env_file:
      - path: ./.env
        required: true
    volumes:
      - ./backend/logs:/app/logs
      - ./data:/data:ro
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    healthcheck:
      disable: true

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${VITE_API_BASE:-/api}
        VITE_APP_ENV: ${VITE_APP_ENV:-production}
        VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID:-}
    container_name: clueark-web
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy

  admin-web:
    build:
      context: ./admin-web
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${ADMIN_VITE_API_BASE:-/api}
    container_name: clueark-admin-web
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy

  crawler:
    build:
      context: ./crawler
      dockerfile: Dockerfile
    container_name: clueark-crawler
    restart: unless-stopped
    env_file:
      - path: ./.env
        required: true
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      # crawler 访问主站使用容器内地址（浏览器/外网不可用）
      CLUEARK_BACKEND_URL: http://backend:3000
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://127.0.0.1:3100/api/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 25s

volumes:
  clueark_mongodb_data:
  clueark_redis_data:
  clueark_qdrant_data:

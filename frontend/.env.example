# API 前缀（Vite 开发代理默认 /api）
# VITE_API_BASE=/api

# Google OAuth Web Client ID（与 backend GOOGLE_CLIENT_ID 一致）
# VITE_GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# 部署环境：development | demo | production
# 未设置时：yarn dev 自动视为 development；生产构建默认为 production
# VITE_APP_ENV=development
